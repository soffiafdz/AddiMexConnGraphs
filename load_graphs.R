
# AUD -----------------------------------------------------------------------------------------

gAUD <- read_rds(paste0(savedir1, "gAUD.rds"))
g.groupAUD <- read_rds(paste0(savedir1, 'g.groupAUD.rds'))


# CON -----------------------------------------------------------------------------------------

gCON <- read_rds(paste0(savedir1, "gCON.rds"))
g.groupCON <- read_rds(paste0(savedir1, 'g.groupCON.rds'))


# DAN -----------------------------------------------------------------------------------------

gDAN <- read_rds(paste0(savedir1, "gDAN.rds"))
g.groupDAN <- read_rds(paste0(savedir1, 'g.groupDAN.rds'))


# DMN -----------------------------------------------------------------------------------------

gDMN <- read_rds(paste0(savedir1, "gDMN.rds"))
g.groupDMN <- read_rds(paste0(savedir1, 'g.groupDMN.rds'))


# FPN -----------------------------------------------------------------------------------------

gFPN <- read_rds(paste0(savedir1, "gFPN.rds"))
g.groupFPN <- read_rds(paste0(savedir1, 'g.groupFPN.rds'))


# MEM -----------------------------------------------------------------------------------------

gMEM <- read_rds(paste0(savedir1, "gMEM.rds"))
g.groupMEM <- read_rds(paste0(savedir1, 'g.groupMEM.rds'))


# SAL -----------------------------------------------------------------------------------------

gSAL <- read_rds(paste0(savedir1, "gSAL.rds"))
g.groupSAL <- read_rds(paste0(savedir1, 'g.groupSAL.rds'))


# SMH -----------------------------------------------------------------------------------------

gSMH <- read_rds(paste0(savedir1, "gSMH.rds"))
g.groupSMH <- read_rds(paste0(savedir1, 'g.groupSMH.rds'))


# SMM -----------------------------------------------------------------------------------------

gSMM <- read_rds(paste0(savedir1, "gSMM.rds"))
g.groupSMM <- read_rds(paste0(savedir1, 'g.groupSMM.rds'))


# SUB -----------------------------------------------------------------------------------------

gSUB <- read_rds(paste0(savedir1, "gSUB.rds"))
g.groupSUB <- read_rds(paste0(savedir1, 'g.groupSUB.rds'))


# UNC -----------------------------------------------------------------------------------------

gUNC <- read_rds(paste0(savedir1, "gUNC.rds"))
g.groupUNC <- read_rds(paste0(savedir1, 'g.groupUNC.rds'))


# VAN -----------------------------------------------------------------------------------------

gVAN <- read_rds(paste0(savedir1, "gVAN.rds"))
g.groupVAN <- read_rds(paste0(savedir1, 'g.groupVAN.rds'))


# VIS -----------------------------------------------------------------------------------------

gVIS <- read_rds(paste0(savedir1, "gVIS.rds"))
g.groupVIS <- read_rds(paste0(savedir1, 'g.groupVIS.rds'))


# Whole brain ---------------------------------------------------------------------------------

gWB <- read_rds(paste0(savedir1, "gWB.rds"))
g.groupWB <- read_rds(paste0(savedir1, 'g.groupWB.rds'))
